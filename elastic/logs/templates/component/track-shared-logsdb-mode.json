{
    "template": {
      "settings": {
        {% if index_mode %}
        "index": {
            "mode": {{ index_mode | tojson }},
            "sort.field": [ "host.name", "@timestamp" ],
            "sort.order": [ "asc", "desc" ]
        }
        {% endif %}
      }
    }
  }