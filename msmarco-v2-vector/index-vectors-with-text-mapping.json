{
  "settings": {
    {# non-serverless-index-settings-marker-start #}{%- if build_flavor != "serverless" or serverless_operator == true -%}
      {% if preload_pagecache %}
    "index.store.preload": [ "vec", "vex", "vem"],
      {% endif %}
    "index.number_of_shards": {{number_of_shards | default(1)}},
    "index.number_of_replicas": {{number_of_replicas | default(0)}},
    "index.merge.scheduler.auto_throttle": {{ index_merge_scheduler_auto_throttle | default(false) | tojson }},
    "index.merge.intra_merge_parallelism_enabled": {{ index_merge_intra_merge_parallelism_enabled | default(false) | tojson }}
    {%- endif -%}{# non-serverless-index-settings-marker-end #}
  },
  "mappings": {
    "properties": {
      "docid": {
        "type": "keyword"
      },
      "title": {
        "type": "text"
      },
      "text": {
        "type": "text"
      },
      "emb": {
        "type": "dense_vector",
        "element_type": {{ vector_index_element_type | default("float") | tojson }},
        "dims": 1024,
        "index": true,
        "similarity": "dot_product",
        "index_options": {
          "type": {{ vector_index_type | default("int8_hnsw") |tojson }}
        }
      }
    }
  }
}
